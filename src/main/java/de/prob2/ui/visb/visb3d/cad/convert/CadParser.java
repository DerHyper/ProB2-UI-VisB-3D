package de.prob2.ui.visb.visb3d.cad.convert;

/**
 * Reads a CAD file (e.g. STL in {@link StlParser}) and returns a 
 * {@link TriangleMeshData}.
 */
interface CadParser {

	// Must Contain:
	// static TriangleMeshData parse(Path file) throws IOException;
}
